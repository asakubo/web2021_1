<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>プレミア所属選手</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.name  %></td>
            <td><%= row.number %></td>
          <td><%= row.name2 %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>